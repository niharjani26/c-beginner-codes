#include<stdio.h>
int main()
{
const int a=8; // now we have const befoe variable type therefore by mistake , the value of a is changed in further programmming then output will be an error
printf("the value of a is %d",a);
 return 0;
}
